# Copia este archivo a .env y completa tu propia API key. Nunca commitees .env.

OPENAI_API_KEY=sk-...

# Opcionales, estos son los defaults si no los seteas:
# OPENAI_MODEL=gpt-4o-mini
# AGENT_DB_PATH=checkpoints.db
# RECURSION_LIMIT=10
